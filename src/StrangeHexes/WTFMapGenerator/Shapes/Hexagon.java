package StrangeHexes.WTFMapGenerator.Shapes;

public class Hexagon extends BaseShape{
    public Hexagon(double rotation, float side_length, float offset_x, float offset_y) {
        super(rotation, side_length, offset_x, offset_y);
    }
}
