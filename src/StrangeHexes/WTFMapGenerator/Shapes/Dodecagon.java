package StrangeHexes.WTFMapGenerator.Shapes;

public class Dodecagon extends BaseShape{
    public Dodecagon(double rotation, float side_length, float offset_x, float offset_y) {
        super(rotation, side_length, offset_x, offset_y);
    }
}
